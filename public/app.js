const $=(id)=>document.getElementById(id);let state=null;const nameOf=(p)=>p.display_name||p.player_name||p.canonical_name||p.tbg_player_id;const ratingOf=(p)=>p.underlying_ability_rating||p.tbg_rating||p.tbgRating||p.rating||"—";const posOf=(p)=>p.position_group||p.positionGroup||p.position_category||p.primary_position||p.position||"—";
function showView(name){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===`${name}View`));document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));}
function renderNav(items){$('clubNav').innerHTML=items.map((item)=>`<a href="#">${item}</a>`).join('');$('clubNav').querySelectorAll('a').forEach((link,index)=>link.addEventListener('click',(e)=>{e.preventDefault();showView(index===1?'squad':index===2?'tactics':index===3?'schedule':'dashboard');}));}
function pick(player,zone,index){return `<label class="player-pick"><input type="checkbox" data-zone="${zone}" value="${player.tbg_player_id}" ${zone==='xi'&&index<11?'checked':''}><span>${nameOf(player)} · ${posOf(player)} · ${ratingOf(player)}</span></label>`;}
function refreshCaptain(){const selected=[...document.querySelectorAll('input[data-zone="xi"]:checked')];$('captain').innerHTML=selected.map(input=>{const player=state.squad.find(row=>row.tbg_player_id===input.value);return `<option value="${input.value}">${nameOf(player)}</option>`;}).join('');}
function render(data){state=data;renderNav(data.navigation);$('managerChip').textContent=data.manager.manager_name;$('clubName').textContent=data.club.canonical_name;$('crest').textContent=(data.club.short_name||data.club.canonical_name).split(/\s+/).map(w=>w[0]).join('').slice(0,3).toUpperCase();$('clubMeta').textContent=`${data.club.division_id?data.club.division_id.replace('division-','Division '):'Unseeded'} · World rank ${data.club.strength?.world_rank||'—'}`;$('nextOpponent').textContent=data.next_fixture.opponent_name;$('fixtureMeta').textContent=data.next_fixture.competition;$('worldName').textContent='TBG World 1';$('worldStatus').textContent=data.world.status;$('nextFixtureCard').textContent=`${data.next_fixture.opponent_name} · ${data.next_fixture.venue}`;$('division').textContent=data.club.division_id?data.club.division_id.replace('division-','Division '):'—';$('rank').textContent=data.club.strength?.world_rank||'—';$('squadCount').textContent=data.squad.length;$('squadRows').innerHTML=[...data.squad].sort((a,b)=>Number(ratingOf(b))-Number(ratingOf(a))).map(p=>`<tr><td>${nameOf(p)}</td><td>${posOf(p)}</td><td>${p.age??'—'}</td><td>${ratingOf(p)}</td><td>${p.assignment_status||'assigned'}</td></tr>`).join('');const ordered=[...data.squad].sort((a,b)=>Number(ratingOf(b))-Number(ratingOf(a)));$('startingXi').innerHTML=ordered.map((p,i)=>pick(p,'xi',i)).join('');$('bench').innerHTML=ordered.map((p,i)=>pick(p,'bench',i)).join('');document.querySelectorAll('input[data-zone="xi"]').forEach(i=>i.addEventListener('change',refreshCaptain));refreshCaptain();}
async function submitDecision(event){event.preventDefault();const payload={manager_id:state.manager.manager_id,club_id:state.club.tbg_club_id,fixture_id:state.next_fixture.fixture_id,formation:$('formation').value,starting_xi:[...document.querySelectorAll('input[data-zone="xi"]:checked')].map(i=>i.value),bench:[...document.querySelectorAll('input[data-zone="bench"]:checked')].map(i=>i.value),captain_id:$('captain').value,set_piece_takers:{penalties:$('captain').value,free_kicks:$('captain').value,corners_left:$('captain').value,corners_right:$('captain').value},tactics:{mentality:$('mentality').value,pressing:$('pressing').value,tempo:$('tempo').value,width:$('width').value,defensive_line:$('defensiveLine').value}};const response=await fetch('/api/decisions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const result=await response.json();$('submissionStatus').className=response.ok?'ok':'error';$('submissionStatus').textContent=response.ok?`Team validated at ${new Date(result.submitted_at).toLocaleString()} · preview only until Supabase is connected`:(result.validation_errors||[result.error]).join(' · ');}
document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));$('decisionForm').addEventListener('submit',submitDecision);fetch('/api/bootstrap').then(r=>r.json()).then(render).catch(error=>{$('clubName').textContent='Could not load world';$('clubMeta').textContent=error.message;});
