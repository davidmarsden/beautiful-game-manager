# World Feed v0 polish follow-up

This follow-up keeps the v0 scope unchanged while improving feel:

- layered Football Pink surfaces replace large white cards;
- successful manager posts prepend only the new feed item;
- successful comments replace only the affected feed card;
- full feed reload remains a fallback if the mutation response cannot return the refreshed item.

No schema or canonical-world changes.
